# 复制为 keystore.properties 并填写密码（该文件已加入 .gitignore，勿提交）
#
# 重要（与「GitHub Release 的 APK 装不上」相关）：
# - app/build.gradle 里只有在「本文件存在」时才会给 release 配 signingConfigs.release。
# - GitHub Actions 检出代码时默认没有 keystore.properties，也没有你的 .jks，
#   若不再做其它处理，历史上会打出「未签名」的 release APK，部分手机会报解析/安装包异常。
# - 当前工程逻辑：没有本文件时，release 会回退为 signingConfigs.debug（可侧载安装）；
#   上架应用商店或要与旧包同签名升级时，必须在本地或 CI（Secrets）里配置本文件 + keystore。
#
storeFile=keystore/cluster-manager-release.jks
keyAlias=cluster_manager
storePassword=你的密钥库密码
keyPassword=你的密钥密码
